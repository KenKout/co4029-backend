Interview transcript (questions asked and the candidate's answers, in order):
{% for turn in transcript %}
Q{{ loop.index }}: {{ turn.question }}
A{{ loop.index }}: {% if turn.answer %}"""
{{ turn.answer }}
"""{% else %}(no answer recorded){% endif %}
{% endfor %}

Judge the candidate against EACH of these learning outcomes (and ONLY these). The "outcome_id" field of each verdict MUST match one of the ids below verbatim:
{% for outcome in outcomes %}
- outcome_id: {{ outcome.outcome_id }}
  outcome: {{ outcome.outcome_text }} ({{ outcome.outcome_type }})
{% endfor %}

Return JSON in the exact shape from the system prompt — one entry per outcome above.
