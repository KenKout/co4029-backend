"""LiveKit voice-interview agent worker (Phase 3).

Standalone process — NOT imported by the FastAPI app. Requires the
``interview-agent`` optional dependency group (``uv sync --extra
interview-agent``). See ``agent.py`` for the entrypoint.
"""
